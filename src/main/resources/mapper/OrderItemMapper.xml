<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.Mapper.OrderItemMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_item (
            order_id, product_id, product_name, product_image, unit,
            price, quantity, total_amount, create_time
        ) VALUES (
            #{orderId}, #{productId}, #{productName}, #{productImage}, #{unit},
            #{price}, #{quantity}, #{totalAmount}, NOW()
        )
    </insert>

    <insert id="batchInsert">
        INSERT INTO order_item (
            order_id, product_id, product_name, product_image, unit,
            price, quantity, total_amount, create_time
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.orderId}, #{item.productId}, #{item.productName}, #{item.productImage}, #{item.unit},
                #{item.price}, #{item.quantity}, #{item.totalAmount}, NOW()
            )
        </foreach>
    </insert>

    <select id="selectByOrderId" resultType="com.example.Entity.OrderItem">
        SELECT * FROM order_item WHERE order_id = #{orderId}
    </select>

</mapper>
