<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.Mapper.OrdersMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `orders` (
            order_no, user_id, merchant_id, merchant_name, total_amount, pay_amount, freight_amount,
            pay_type, pay_status, order_status,
            receiver_name, receiver_phone, receiver_address,
            remark, create_time, update_time
        ) VALUES (
            #{orderNo}, #{userId}, #{merchantId}, #{merchantName}, #{totalAmount}, #{payAmount}, #{freightAmount},
            #{payType}, #{payStatus}, #{orderStatus},
            #{receiverName}, #{receiverPhone}, #{receiverAddress},
            #{remark}, NOW(), NOW()
        )
    </insert>

    <select id="selectById" resultType="com.example.Entity.Orders">
        SELECT * FROM `orders` WHERE id = #{id}
    </select>

    <select id="selectByOrderNo" resultType="com.example.Entity.Orders">
        SELECT * FROM `orders` WHERE order_no = #{orderNo}
    </select>

    <select id="selectByUserId" resultType="com.example.Entity.Orders">
        SELECT * FROM `orders`
        WHERE user_id = #{userId}
        <if test="orderStatus != null">
            AND order_status = #{orderStatus}
        </if>
        ORDER BY create_time DESC
    </select>

    <update id="updatePaymentStatus">
        UPDATE `orders`
        SET pay_status = #{payStatus},
            order_status = #{orderStatus},
            pay_time = #{payTime},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateOrderStatus">
        UPDATE `orders`
        SET order_status = #{orderStatus},
            cancel_time = #{cancelTime},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateOrderStatusWithReceiveTime">
        UPDATE `orders`
        SET order_status = #{orderStatus},
            receive_time = #{receiveTime},
            update_time = NOW()
        WHERE id = #{id}
    </update>

</mapper>
